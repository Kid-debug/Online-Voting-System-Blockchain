<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password Link Expired</title>
    <style>
      body {
        font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
        background: linear-gradient(rgba(1, 11, 11, 0.5), rgba(10, 10, 10, 0.5)),
          url("http://localhost:5173/public/vote.jpg");
        background-position: center;
        background-size: cover;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        text-align: center;
      }

      .card {
        background: rgba(255, 255, 255, 0.98);
        border-radius: 15px;
        box-shadow: 0px 8px 20px 0px rgba(31, 38, 135, 0.37);
        padding: 40px;
        max-width: 400px;
        width: calc(100% - 40px);
        margin: 20px;
        backdrop-filter: blur(5px);
        border: 1px solid rgba(255, 255, 255, 0.18);
      }

      .card img {
        max-width: 100px;
        margin-bottom: 20px;
      }

      h1 {
        font-size: 28px;
        color: #333;
        margin-bottom: 16px;
      }

      p,
      ol,
      li {
        font-size: 16px;
        color: #666;
        margin-bottom: 24px;
        line-height: 1.6;
      }

      li {
        text-align: left;
      }

      button {
        background-color: #28a745;
        color: white;
        border: none;
        border-radius: 5px;
        padding: 12px 30px;
        font-size: 16px;
        cursor: pointer;
        transition: background-color 0.3s ease;
        outline: none;
        font-weight: 600;
      }

      button:hover {
        background-color: #218838;
      }

      .message {
        background-color: #ffc107;
        color: #212529;
        padding: 10px;
        border-radius: 5px;
        margin-top: 15px;
        font-size: 14px;
      }

      /* Responsive styles */
      @media (max-width: 768px) {
        .card {
          padding: 20px;
          max-width: 350px;
        }
      }

      @media (max-width: 576px) {
        .card {
          padding: 15px;
          max-width: 300px;
        }

        h1 {
          font-size: 22px;
        }

        p,
        ol,
        li,
        .message {
          font-size: 14px;
        }

        button {
          padding: 10px 20px;
          font-size: 14px;
        }

        .card img {
          max-width: 80px;
        }
      }
    </style>
  </head>
  <body>
    <div class="card">
      <img
        src="https://cdn-icons-png.flaticon.com/512/107/107817.png"
        alt="Broken Link"
      />
      <h1><%= message %></h1>
      <p><strong>To reset your password, please follow these steps:</strong></p>
      <ol>
        <li>
          Click the button below in order to redirect to the Forgot Password
          page.
        </li>
        <li>Enter your email to request a new reset password link.</li>
      </ol>
      <button onclick="redirectToForgotPassword()">
        Go To Forgot Password Page
      </button>
    </div>

    <script>
      function redirectToForgotPassword() {
        window.location.href = "http://localhost:5173/forgotPass";
      }
    </script>
  </body>
</html>
